# Docker Compose Environment Variables
# Kopiere diese Datei zu .env und passe die Werte an

# Port-Konfiguration
COMPOSE_PORT=8000

# Volume-Pfade (relativ zum docker-compose.yml Verzeichnis)
COMPOSE_CONFIG_DIR=./config

# Session Secret (OPTIONAL: Wird automatisch generiert wenn nicht gesetzt)
# Die App generiert beim ersten Start automatisch ein sicheres Secret in /config/config.yaml
# Nur setzen, wenn du ein spezifisches Secret verwenden möchtest (z.B. für mehrere Container)
# Generiere einen sicheren Secret mit: openssl rand -hex 32
# SESSION_SECRET=

# Optional: Überschreibe App-interne Umgebungsvariablen
# (Normalerweise nicht nötig, da die App automatisch /config verwendet)
# CONFIG_PATH=/config/config.yaml
# AUTH_FILE=/config/auth.yaml
# ENCRYPTION_KEY_PATH=/config/.encryption_key
# AUDIT_LOG_FILE=/config/audit.log
